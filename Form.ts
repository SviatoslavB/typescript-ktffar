import { Kreis } from "./Kreis";

export abstract class Form{
 
  farbe: String;

  constructor (farbe: String){
    this.farbe = farbe;
  }

  berechneFlaeche(form: Form){
    if(Form = Kreis)
   return 0;
  }

}